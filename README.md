# Domainjs

### A simple administrative tool that can help in grouping multiple domains under a known server (IP).

Features:
 - Shows a warning when a resolved domain suddenly resolved to a different server